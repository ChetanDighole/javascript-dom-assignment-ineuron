// Declare a string variable with trailing whitespace
const myString = "    Hello, World!    ";

// Use trim() method to remove the trailing whitespace
const trimmedString = myString.trim();

// Display the result
console.log(trimmedString); // Output: "Hello, World!"
