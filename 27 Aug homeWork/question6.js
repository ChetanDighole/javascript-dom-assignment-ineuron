// Declare a string variable
const myString = "Facebook, Google, Microsoft, Apple, IBM, Oracle, Amazon";

// Split the string at the comma and convert it to an array
const myArray = myString.split(", ");

// Display the result
console.log(myArray); // Output: ["Facebook", "Google", "Microsoft", "Apple", "IBM", "Oracle", "Amazon"]
