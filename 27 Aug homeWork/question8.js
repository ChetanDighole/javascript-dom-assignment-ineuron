// Declare a string variable
const mySentence = "You cannot end a sentence with because because because is a conjunction";

// Use search() method to find the position of the first occurrence of the word "because"
const firstPosition = mySentence.search("because");

// Display the result
console.log(firstPosition); // Output: 30
